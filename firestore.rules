rules_version = '2';
// 仮ルール（Step 4.5 で全コレクションに対する強化版を実装する）
// PoC初動チェック用：認証必須のみ
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
